export interface Pipe {
    x: number;
    topHeight: number;
    bottomHeight: number;
    passed: boolean;
}
  
export interface BirdRect {
    x: number;
    y: number;
    width: number;
    height: number;
}